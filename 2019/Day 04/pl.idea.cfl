; Garbage for now, might make a PL

main &args ->
  fn -> &&.lift(^.set.bind(!), ^.sort.bind(=))
  xs -> fn.filter([136760..595730].map(^.str))
  xs.size.printf("Silver: %d\n")
  (^.group.map(^.size).elem(2)).filter(xs)
    .size.printf("Gold: %d\n")
